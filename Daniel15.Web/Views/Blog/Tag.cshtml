@model Daniel15.Web.ViewModels.Blog.TagListingViewModel

@{
	Model.PageUrlGenerator = page => Url.Action(MVC.Blog.Tag(Model.Tag.Slug, page));
	Model.Heading = Config.BlogName;
	Model.Title = string.Format("Latest posts in {0} — {1}", Model.Tag.Title, Config.BlogName);

	// Only show page number if we're not on page 1
	if (Model.Page != 1)
	{
		Model.Title = string.Format("Page {0} — {1}", Model.Page, Model.Title);
	}
}

@Html.Partial(MVC.Blog.Views._Posts, Model)