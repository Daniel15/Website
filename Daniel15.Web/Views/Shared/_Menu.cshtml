@model List<Daniel15.Web.Models.Shared.MenuItemModel>
<nav>
	<ul>
		@foreach (var item in Model)
		{
			<li@(item.Active ? " class=\"selected\"" : string.Empty)><a href="@item.Url">@item.Title</a></li>
		}
	</ul>
</nav>